---
title: 遇到问题
order: 3
description: 反馈问题时需提供的材料：当天的日志、「复制诊断信息」的内容，以及常见情况的自查。
---

问题与建议请提交到本项目自己的问题渠道（待维护者填写仓库地址）；无论走哪里，反馈时都请附上下面这些材料。

## 反馈时请提供

1. 「偏好设置 → 关于 → 复制诊断信息」：版本、系统、加载的数据、配置原文、日志目录，写入剪贴板。API 密钥会被抹去。
2. 「关于 → 打开日志目录」中当天的日志文件（Windows 上另附「%LOCALAPPDATA%\Manbo」下当天的「tsf.日期.log」）。
3. 出现问题时所在的应用、输入的内容、预期结果与实际结果。

可复现时，先打开「高级 → 详细日志」再复现一次，日志中即有逐键记录；排查完成后请关闭。

## 常见情况

- **安装后输入法列表中没有曼波**：macOS 到「系统设置 → 键盘 → 输入法 → 编辑 → +」在简体中文下添加，仍没有则注销后重新登录；
  Windows 注销后重新登录。重启后仍然没有，请反馈给维护者并附系统版本与安装方式。
- **任务栏搜索框、「设置」中看不到候选窗口**（Windows）：测试版没有代码签名，候选窗口会被这类系统应用遮挡，为已知问题，将在签名版中解决；上屏本身正常。
- **某个应用中无法切换到曼波或输入无反应**（Windows）：注销后重新登录，或重启该应用。
- **候选窗口遮挡了终端 / 编辑器自身的补全**：英文模式在这些应用中缺省不给候选；被遮挡的是中文候选时，切换到英文模式即可。
- **登录后当前输入法不是曼波**（macOS）：已知问题，手动切换一次即可，待签名版解决。
- **悬浮状态条不见了**（Windows）：它只在当前输入法为曼波时显示；确认「设置 → 候选窗口」中的开关已打开。它每次显示时都会被移回屏幕可见范围内，不会停留在屏幕之外。
- **配置文件改错**：macOS 输入法菜单会增加一条灰色的错误说明，此时沿用上一份可用的配置；按说明改回并保存即可。
- **某个应用中表现异常**（拼音渲染异常、按键被吞、无法读取选区）：请将应用名称与现象反馈给作者；可先在「候选窗口」页更换拼音显示位置作为临时处理。
