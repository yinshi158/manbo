---
title: 数据与日志
order: 1
description: 曼波在本机保存的文件及其内容、云联想发送的内容、输入日志的关闭与清空、运行日志的内容。
---

曼波不上传任何数据。拼音转换、词库、学习、释义全部在本机完成，没有账号，没有统计上报。

## 本机文件

- **macOS**：「~/Library/Application Support/Manbo/」（访达中按 `⇧⌘G` 输入此路径）。
- **Windows**：「%APPDATA%\Manbo」（在资源管理器地址栏中直接输入此路径）。

均为纯文本，可随时打开查看：

| 文件 | 内容 | 是否含输入内容 |
|---|---|---|
| `config.toml` | 配置 | 否 |
| `.env` | 云服务的密钥，填写后才有；仅当前账户可读 | 否 |
| `user.tsv` | 每个词被选择的次数 | 选过的词 |
| `user-words.tsv` | 自动造词与云端选中的用户词 | 选过的词 |
| `user-choices.tsv` | 某段拼音下选过的词 | 拼音与选过的词 |
| `user-english.tsv` | 原样上屏过的英文词 | 英文词 |
| `user-typos.tsv` | 被接受的输入错误纠正（输成了什么、本意是什么） | 拼音音节 |
| `user-ngram.tsv` | 词与词之间的接续 | 连续输入过的词 |
| `user-glossary-<语言>.tsv` | 云端补充的个人释义 | 词与译词 |
| `user-vocab.tsv` | 每条译词被看到 / 上屏 / 输出的次数 | 译词，不含中文 |
| `usage.tsv` | 按天的汉字 / 词 / 上屏次数，仅数字 | 否 |
| `input-log.jsonl` | 输入日志：每次上屏的按键、切分、候选、所选序号，以及退格重打、直通的标点、云端联想给过什么、所在应用 | 按键与上屏的文字 |
| `dicts/` | 用户导入的词库 | 否 |
| `sync/` | 同步的状态与基准快照（开了「同步」才有） | 与被同步的那几份文件相同 |

这些文件每次整体替换，即使输入法中途崩溃也不会留下写到一半的文件。

开启「同步」后，上表中除 `input-log.jsonl` 与 `.env` 之外的文件会按「帮助 → 同步」里
配置的后端合并到你在坚果云 / Nextcloud / 同步文件夹中的数据，用来在多台设备之间保留
输入习惯；输入日志与密钥永远不参与同步。详见「帮助 → 同步」。

## 云联想发什么

缺省关闭。打开后，输入拼音时的拼音、光标前后各几十个字的上下文、本机给出的候选，会发送给用户在「云服务」页填写的 AI 服务商。
数据直接从本机发送到服务商，不经过曼波：曼波没有自己的服务器。

- 密码框中不发送；应用声明为私密的输入框（Windows 上输入范围为私密 / 密码 / PIN 的）里也不发送。
- 开启期间，随包释义表中没有译词的词在输出后单独发送一次，只含该词。
- 关闭「云服务」页的开关后不再发送任何请求。

## 输入日志

测试版缺省开启。每次上屏在本机记录一行：按键、切分、看到的第一页候选、所选序号、翻了几页、从第一键到上屏用了多久、所在应用的标识
（macOS 为 bundle identifier，Windows 为 exe 名）。另有几类事件各记一行：组句中退格后重打了不同的键、组句之外直接打出的标点与回车、
云端联想给出的候选与整句、切换应用或点到别处、输入法启动。只含通过曼波打出的内容，不含应用中已有的文字，不含密码框与应用声明为私密的输入框中的任何内容，那里也不学习习惯。
它只保存在本机，用于离线回归评测与个人模型，不会自动发送给任何人。

- 关闭：「偏好设置 → 高级」取消勾选「记录输入日志」。
- 清空：同一页的「清空输入日志」。
- 测试期间若愿意将此文件提供给作者，对改进排序有很大帮助；完全自愿。

## 运行日志

- **macOS**：「~/Library/Logs/Manbo/」。
- **Windows**：「%APPDATA%\Manbo\logs」；此外每个应用内的输入法部分另写一份到「%LOCALAPPDATA%\Manbo」下的「tsf.日期.log」，同样按天分文件、保留 7 天。

按天分文件，保留 7 天。缺省级别不记录输入内容；仅在「高级」页打开「详细日志」后才逐键记录，用于排查问题，排查完成后请关闭。
