# 样例数据

手写的小词库，只用于 CLI 演示和测试，不是产品词库（产品词库源在 `assets/lexicon/`）。不涉及第三方许可。

- `dict.tsv`：`词\t音节\t词频`
- `glossary-en.tsv` / `glossary-ja.tsv`：`词\t[词性. ]译文[\t[词性. ]译文]`，每个文件一种学习语言
- `english.tsv`：英文模式候选用的词表，`词\t编码\t词频`（与 `data/generated/english.tsv` 同格式）
