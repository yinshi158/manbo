# 复制为 .env 后按需修改。所有变量都有默认值，缺失不报错。

# 日志级别过滤（tracing-subscriber EnvFilter 语法）
RUST_LOG=info,manbo_core=debug

# 日志文件目录，按天滚动；留空则只输出到终端
MANBO_LOG_DIR=logs

# 学习语言：en / ja
MANBO_LEARNING_LANGUAGE=en

# 学习数据同步的 WebDAV 密码（[sync] password 留空时读这个；坚果云用「应用密码」，不是登录密码）
# MANBO_SYNC_PASSWORD=
